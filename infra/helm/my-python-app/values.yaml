replicaCount: 1
image:
  repository: iriscafe50/api-golang
  tag: 
service:
  name: golang-api
  namespace:
    create: true
  type: ClusterIP
  port: 8080
ingress:
  enabled: false
  hosts:
    - host: golang-api.local
      paths:
        - /buckets
resources:
  requests:
    memory: "64Mi"
    cpu: "250m"
  limits:
    memory: "128Mi"
    cpu: "500m"