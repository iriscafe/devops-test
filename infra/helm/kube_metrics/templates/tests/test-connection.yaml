apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kube_metrics.fullname" . }}-test-connection"
  labels:
    {{- include "kube_metrics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kube_metrics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
