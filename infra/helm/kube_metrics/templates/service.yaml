apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube_metrics.fullname" . }}
  labels:
    {{- include "kube_metrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube_metrics.selectorLabels" . | nindent 4 }}
